import 'package:flutter/material.dart';

class CaregiverConfigurationPortalScreen extends StatelessWidget {
  const CaregiverConfigurationPortalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Caregiver Configuration Portal'),
      ),
      body: Center(
        child: const Text('Caregiver Configuration Portal Screen'),
      ),
    );
  }
}
